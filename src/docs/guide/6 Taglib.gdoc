h2. :init

Usage: <facebookAuth:init />

Puts Facebook Javascript SDK initialization code. Puts this code only once per page, feel free to call
it when you need

h2. :connect

Usage: <facebookAuth:connect />

Puts Facebook Connect button. If Facebook Javascript SDK initialization code wasn't added into current page,
 then calling of this tag will call @:init@ automatically.

